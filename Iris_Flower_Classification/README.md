# Iris Flower Classification 🌸

## Project Overview

This project demonstrates a complete **Machine Learning workflow** using the famous **Iris dataset**.
The objective is to build and compare multiple classification models that can predict the **species of an iris flower** based on its physical measurements.

The dataset contains measurements of **sepal length, sepal width, petal length, and petal width** for three species of iris flowers.

The models are trained and evaluated using standard machine learning practices including **train-test split, model evaluation, and model comparison**.

---

## Problem Statement

The goal of this project is to build a machine learning model that can classify iris flowers into one of the following species:

* Setosa
* Versicolor
* Virginica

based on their petal and sepal measurements.

---

## Dataset Information

The dataset used in this project is the **Iris dataset**, which is included in the Scikit-Learn library.

Dataset characteristics:

* Total samples: **150**
* Number of features: **4**
* Number of classes: **3**

Features used:

* Sepal Length
* Sepal Width
* Petal Length
* Petal Width

Target variable:

* Species

---

## Project Structure

```
01_Iris_Classification
│
├── notebooks
│   └── iris_classification.ipynb
│
├── models
│   └── iris_best_model.pkl
│
├── data
│
└── README.md
```

---

## Machine Learning Workflow

The following steps were followed in this project:

1. Import required libraries
2. Load the dataset
3. Data understanding
4. Exploratory Data Analysis (EDA)
5. Feature and target separation
6. Train-test split
7. Model training
8. Model evaluation
9. Model comparison
10. Confusion matrix visualization
11. Save the best model

---

## Models Used

The following machine learning algorithms were trained and evaluated:

* Logistic Regression
* K-Nearest Neighbors (KNN)
* Decision Tree
* Random Forest
* Support Vector Machine (SVM)

---

## Evaluation Metrics

The models were evaluated using the following metrics:

* Accuracy
* Precision
* Recall
* F1-score
* Confusion Matrix

---

## Results

Most models performed very well on this dataset due to the clear separation between classes.
The best-performing model was saved for future use.

---

## Technologies Used

* Python
* Pandas
* NumPy
* Matplotlib
* Seaborn
* Scikit-Learn
* Jupyter Notebook

---

## Key Learnings

This project demonstrates the complete machine learning pipeline including:

* Data exploration
* Model training
* Model evaluation
* Model comparison
* Saving trained models

It provides a strong foundation for building more complex machine learning projects.

---

## Future Improvements

Possible improvements include:

* Hyperparameter tuning
* Cross-validation
* Model deployment
* Creating a prediction API

---

## Author

Jitender
